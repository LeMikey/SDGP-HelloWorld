import 'package:flutter/material.dart';

class CoummunityPage extends StatelessWidget {
  const CoummunityPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
